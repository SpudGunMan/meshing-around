# To activate meshtastic virtual enviroment:
cd ~
source /home/scot/Projects/WeatherWarn/venv/bin/activate

# Run weatherwarn app
cd Projects/WeatherWarn
streamlit run app.py

deactivate

# PAT for scot@wilcoxon.org Git updates
# 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

# To add current dir changes, commit, and push to repo:
git add .
git commit -m "this thing finally works'
git push origin main

# To emit list of files as meshing-around command fragments:
ls | awk '{ printf "\"%s\": lambda: read_file(\"datafiles/regions/%s\", True),\n", $1, $1 }' >out.txt

